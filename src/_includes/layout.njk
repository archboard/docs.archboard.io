<!DOCTYPE html>
<html lang="{{ locale }}" dir={{ dir }}>
<head>
  {% include "head.njk" %}
  {% block head %}{% endblock %}
</head>
<body class="bg-gradient-to-br from-gray-200 to-white dark:from-gray-900 dark:to-gray-700 min-h-screen text-gray-900 dark:text-gray-100">
  <div class="max-w-3xl mx-auto px-6">
    {% include 'header.njk' %}

    <main>
      {% block body %}
        {{ content | safe }}
      {% endblock %}
    </main>

    {% include "footer.njk" %}
  </div>
</body>
</html>
